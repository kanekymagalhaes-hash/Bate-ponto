// No celular, "localhost" aponta para o próprio aparelho. Informe aqui o IP do computador
// que está executando `npm.cmd start` na raiz do projeto (veja com `ipconfig`).
export const API_URL = 'http://192.168.3.11:3000';
